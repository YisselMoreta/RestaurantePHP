{% extends 'base.html.twig' %}

{% block body %}
    <h1>Plato</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plato.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ plato.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ plato.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('platos_index') }}">Atrás</a>
        </li>
        <li>
            <a href="{{ path('platos_edit', { 'id': plato.id }) }}">Editar</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Eliminar">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
